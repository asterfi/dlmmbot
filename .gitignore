node_modules/
dist/
data/
coverage/
.deploy-quarantine/
.deploy-verify/
.env
.env.*
!.env.example
*.log
HALT
PAUSE
secrets/
# Live credential material and editor/tool backups — never committable.
# credentials/ holds live.env; *.bak* are pre-change safety copies.
credentials/
*.bak
*.bak-*
run-service.sh
.claude/
tmp-*

# Daily X video (video/): rendered output and captured dashboard shots are
# regenerated by `npm run daily` — the source and the pipeline are versioned.
video/out/
video/public/shots/
video/public/icons/
video/public/daily.json
video/public/mascot-*.png
video/public/sfx/
# X Article review artifact (676 KB, image embedded as base64) — regenerable from the .md + png; the sources are tracked
docs/article/article-review.html
gmgn-pace.json
